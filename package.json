{
  "name": "discord-wow-request-system",
  "version": "1.0.0",
  "description": "simple request and deposit system for discord",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "create_db": "cross-var createdbjs $DB_NAME --user=$DB_USERNAME --password=$DB_PASSWORD",
    "migrate": "node-pg-migrate"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cross-var": "^1.1.0",
    "discord.js": "^11.5.1",
    "dotenv": "^8.0.0",
    "husky": "^2.4.1",
    "node-pg-migrate": "^3.21.1",
    "pg": "^7.11.0",
    "pgtools": "^0.3.0",
    "pretty-quick": "^1.11.1"
  },
  "devDependencies": {
    "prettier": "1.18.2"
  },
  "husky": {
    "hooks": {
      "pre-commit": "pretty-quick --staged"
    }
  }
}
